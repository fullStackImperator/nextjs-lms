/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "uploadthing.com",
            "utfs.io",
            "images.unsplash.com",
        ]
    }
}

module.exports = nextConfig
